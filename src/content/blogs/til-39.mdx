---
title: "something magic move"
pubDate: 2026-03-04
isFeatured: false
tags: ['typescript']
description: It was about time that I came up with an idea for a package. Even if it's a small one.
---

## What is it?

As mentioned above, the main goal of the plugin was to add language logos to the bottom right of any code block.

```ts magic-move-before={1-5} magic-move-after={6-12} hide-badge
type User = {
  name: string;
  age: number;
  role: 'admin' | 'user';
};
type User = {
  id: string;
  name: string;
  age: number;
  role: 'admin' | 'user' | "editor";
  sessionId: string;
};
```

something text

```go hide-badge magic-move-before={1-24} magic-move-after={25-50} magic-move-duration=750 magic-move-line-numbers=false magic-move-stagger=5
package main

import (
    "fmt"
    "net/http"
    "time"
)

type Server struct {
    port     string
    handlers map[string]http.HandlerFunc
}

func NewServer(port string) *Server {
    return &Server{
        port:     port,
        handlers: make(map[string]http.HandlerFunc),
    }
}

func (s *Server) Start() error {
    fmt.Printf("Server starting on port %s\n", s.port)
    return http.ListenAndServe(":"+s.port, nil)
}
package main

import (
    "fmt"
    "net/http"
    "time"
)

type Server struct {
    port     string
    timeout  time.Duration
    handlers map[string]http.HandlerFunc
}

func NewServer(port string) *Server {
    return &Server{
        port:     port,
        timeout:  30 * time.Second,
        handlers: make(map[string]http.HandlerFunc),
    }
}

func (s *Server) Start() error {
    fmt.Printf("Server starting on port %s\n", s.port)
    return http.ListenAndServe(":"+s.port, nil)
}
```
