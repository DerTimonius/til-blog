---
// eslint-disable-next-line no-unused-vars
interface Props {
  header?: string;
}

const { header } = Astro.props;
---

<section
  class="min-h-min border-2 border-dashed border-custom-accent px-6 py-2 text-current"
>
  {header ? <h3 class="mt-3">{header}</h3> : null}
  <slot />
</section>
