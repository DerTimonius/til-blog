---
const currentUrlPath = Astro.url.pathname.replace(/\/+$/, '');
const breadcrumbList = currentUrlPath.split('/').slice(1);
const breadcrumbListLength = breadcrumbList.length;

const crumbClasses =
	'inline-block py-[0.35rem] px-3 border-2 border-transparent text-base no-underline opacity-70 transition-all duration-150 hover:opacity-100 hover:border-line hover:bg-card hover:shadow-[2px_2px_0_0_rgb(var(--color-text-base))] hover:-translate-x-px hover:-translate-y-px';

const crumbActiveClasses =
	'inline-block py-[0.35rem] px-3 border-2 border-line bg-accent text-inverted';
---

<nav class="mx-auto mb-6 mt-8 w-full max-w-4xl px-4 sm:px-6" aria-label="breadcrumb">
  <ul class="flex flex-wrap items-center gap-2 font-ui text-sm font-medium uppercase tracking-wider">
    <li class="inline">
      <a 
        class={crumbClasses} 
        href="/"
      >
        Home
      </a>
    </li>
    <li aria-hidden="true" class="text-accent">
      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="square" stroke-linejoin="miter">
        <polyline points="9 18 15 12 9 6"></polyline>
      </svg>
    </li>
    {
      breadcrumbList.map((breadcrumb, index) =>
        index + 1 === breadcrumbListLength ? (
          <li>
            <span 
              class={crumbActiveClasses}
              aria-current="page"
            >
              {breadcrumb.replace(/-/g, ' ')}
            </span>
          </li>
        ) : (
          <>
            <li>
              <a 
                href={`/${breadcrumb}`}
                class={crumbClasses}
              >
                {breadcrumb.replace(/-/g, ' ')}
              </a>
            </li>
            <li aria-hidden="true" class="text-accent">
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="square" stroke-linejoin="miter">
                <polyline points="9 18 15 12 9 6"></polyline>
              </svg>
            </li>
          </>
        ),
      )
    }
  </ul>
</nav>
